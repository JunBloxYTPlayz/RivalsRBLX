local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("DurexHub", "DarkTheme")
local Tab = Window:NewTab("Main")
local Section = Tab:NewSection("Main Features")

-- Auto Aim Button
Section:NewButton("Auto Aim", "Best Auto Aim", function()
    local success, err = pcall(function()
        loadstring(game:HttpGet(('https://pastebin.com/raw/3t1v5Zve')))()
    end)
    if not success then
        warn("Failed to load Auto Aim script: " .. err)
    else
        print("Auto Aim button clicked")
    end
end)

-- Player ESP Button
Section:NewButton("Player ESP", "ButtonInfo", function()
    local success, err = pcall(function()
        loadstring(game:HttpGet(('https://pastebin.com/raw/JF7jpTwn')))()
    end)
    if not success then
        warn("Failed to load Player ESP script: " .. err)
    else
        print("Player ESP button clicked")
    end
end)

-- Infinite Jump Toggle
local InfiniteJumpEnabled = true
Section:NewToggle("Infinite Jump", "ToggleInfo", function(state)
    if state then
        print("Infinite Jump On")
        InfiniteJumpEnabled = true
    else
        print("Infinite Jump Off")
        InfiniteJumpEnabled = false
    end
end)

game:GetService("UserInputService").JumpRequest:Connect(function()
    if InfiniteJumpEnabled then
        local character = game:GetService("Players").LocalPlayer.Character
        if character and character:FindFirstChildOfClass('Humanoid') then
            character:FindFirstChildOfClass('Humanoid'):ChangeState("Jumping")
        end
    end
end)

-- Noclip Toggle
local Noclip = nil
local Clip = nil

function noclip()
    Clip = false
    local function Nocl()
        if Clip == false and game.Players.LocalPlayer.Character ~= nil then
            for _,v in pairs(game.Players.LocalPlayer.Character:GetDescendants()) do
                if v:IsA('BasePart') and v.CanCollide and v.Name ~= floatName then
                    v.CanCollide = false
                end
            end
        end
        wait(0.
